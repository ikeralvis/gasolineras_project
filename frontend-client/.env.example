# ==============================
# Frontend base config
# ==============================
# API Gateway base URL (recommended single entrypoint)
VITE_API_BASE_URL=http://localhost:8080

# ==============================
# Optional direct microservice URLs
# (useful for local debugging, gateway fallbacks, or diagnostics)
# ==============================
VITE_GASOLINERAS_API_URL=http://localhost:8000
VITE_RECOMENDACION_API_URL=http://localhost:8086
VITE_USUARIOS_API_URL=http://localhost:3001
VITE_CHARGING_API_URL=http://localhost:8091

# Voice base URL (used to auto-build websocket URL when VITE_VOICE_WS_URL is empty)
VITE_VOICE_SERVICE_URL=http://localhost:8090

# Voice assistant websocket (takes precedence if set).
# Local example: ws://localhost:8090/ws/voice
# Cloud Run example: wss://voice-assistant-xxxxx-uc.a.run.app/ws/voice
VITE_VOICE_WS_URL=

# Enable websocket transport for voice assistant.
# - true: try /api/voice/ws first and fallback to HTTP on failure
# - false: force HTTP only (/api/voice/dialog)
# Default behavior when unset:
# - development: true
# - production: true only if VITE_VOICE_WS_URL is explicitly set
VITE_VOICE_WS_ENABLED=

# ==============================
# Google OAuth
# ==============================
# Obtain from Google Cloud Console > APIs & Services > Credentials
VITE_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# ==============================
# Maps and tiles
# ==============================
# Default app tiles if unset: OpenStreetMap standard tiles (free, no API key)
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
VITE_MAP_TILE_ATTRIBUTION=&copy; <a href='https://www.openstreetmap.org/copyright'>OpenStreetMap</a> contributors

# Optional map provider token(s)
VITE_MAPBOX_TOKEN=
