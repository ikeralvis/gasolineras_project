# ====================================
# CONFIGURACIÓN DEL SERVIDOR
# ====================================
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# ====================================
# BASE DE DATOS POSTGRESQL
# ====================================
# Cuando uses Docker Compose, DB_HOST debe ser 'postgres' (nombre del contenedor)
# En desarrollo local, usa 'localhost'
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_seguro
DB_NAME=usuarios_db

# ====================================
# AUTENTICACIÓN JWT
# ====================================
# ⚠️ IMPORTANTE: Cambia este secreto por uno aleatorio y seguro en producción
# Puedes generarlo con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambiar_por_secreto_aleatorio_seguro
JWT_EXPIRES_IN=7d

# ====================================
# NOTAS
# ====================================
# - JWT_SECRET: Mínimo 32 caracteres, único por ambiente
# - JWT_EXPIRES_IN: Formato: 60s, 10m, 2h, 7d
# - DB_HOST: 'postgres' en Docker, 'localhost' en desarrollo local
